interface:
  display_name: "Work Charter"
  short_description: "Recommend safe, proportional project coordination"
  default_prompt: "Use $work-charter to recommend how this consequential project work should continue, then ask before inspecting project details."

policy:
  allow_implicit_invocation: true
